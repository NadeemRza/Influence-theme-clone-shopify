{% if section.settings.collection_id != blank %}
  <div class="featured-collection">
    <div class="featured-collection__heading h2 heading1">Best Selling Dresses</div>
    {% assign product_limit = section.settings.product_limit %}
    <div class="flex justify-between mt-1">
      {% for product in section.settings.collection_id.products %}
        <div class="featured-collection__content">
          <div class="featured-collection__card">
            {% render 'product-card-1', product: product %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
{% endif %}

{% style %}
  .heading1 {
    color: {{ section.settings.heading_text_color}}
    ;
    text-align: {{ section.settings.heading_position}}
    ;
  }
{% endstyle %}


{% schema %}
  {
    "name": "Featured-Collection",
    "class": "Featured-Collection",
    "tag": "section",
    "settings": [
      {
        "type": "collection",
        "id": "collection_id",
        "label": "Collection"
      }, {
        "type": "range",
        "id": "product_limit",
        "min": 1,
        "max": 20,
        "step": 1,
        "label": "Product Limit",
        "default": 8
      }, {
        "type": "color",
        "id": "heading_text_color",
        "label": "Heading Color",
        "default": "#261d09"
      }, {
        "type": "select",
        "id": "heading_position",
        "label": "Heading position",
        "options": [
          {
            "value": "left",
            "label": "Left"
          }, {
            "value": "center",
            "label": "Center"
          }, {
            "value": "right",
            "label": "Right"
          }
        ],
        "default": "center"
      }
    ],
    "presets": [
      {
        "name": "Featured collection"
      }
    ]
  }
{% endschema %}